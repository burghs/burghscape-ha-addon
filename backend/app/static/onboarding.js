(function(){"use strict";
const steps=[
{title:"Welcome to MyBeacon",text:"A quick tour will show you your Home Assistant status, backup protection, support, announcements, appearance controls, and Getting Started.",target:null},
{title:"Home Assistant status",text:"See whether your system is online and review key Home Assistant details here.",target:"instance"},
{title:"Backup protection",text:"Review managed and Home Assistant backup history and protection status.",target:"backups"},
{title:"Support and help",text:"Open support tickets, contact support, and download a diagnostic report.",target:"support"},
{title:"Announcements",text:"What’s New contains service announcements and offers intended for your account.",target:"campaigns"},
{title:"Account and appearance",text:"Use Account to change your password or choose System, Light, or Dark appearance.",target:"account"},
{title:"Guides & Help",text:"Find step-by-step instructions for connecting the Home Assistant app, using your MyBeacon URL and resolving common setup issues.",target:"guides"},
{title:"Getting Started",text:"Return to Getting Started for setup guidance, terminology, your checklist, or to replay this tour.",target:"getting-started"},
{title:"You are ready",text:"Your portal is ready. Promotions resume under their normal eligibility rules after you finish or skip.",target:null}
];
const modal=document.getElementById("onboarding-modal");if(!modal){window.dispatchEvent(new CustomEvent("onboarding:ready",{detail:{active:false}}));return}
const panel=modal.querySelector("[role=document]"),spotlight=document.getElementById("onboarding-spotlight"),dimmers=[...document.querySelectorAll("[data-onboarding-dimmer]")],title=document.getElementById("onboarding-title"),text=document.getElementById("onboarding-text"),progress=document.getElementById("onboarding-progress"),back=document.getElementById("onboarding-back"),next=document.getElementById("onboarding-next"),skip=document.getElementById("onboarding-skip");let current=0,active=false,previousFocus=null,target=null,positionFrame=0;
async function api(path,method="POST",body){const r=await fetch("/api/portal/onboarding"+path,{method,credentials:"include",headers:body?{"Content-Type":"application/json"}:{},body:body?JSON.stringify(body):undefined});if(!r.ok)throw new Error("Onboarding request failed");return r.json()}
function focusables(){return [...panel.querySelectorAll("button:not([disabled]),a[href]")].filter(x=>x.offsetParent!==null)}
function viewport(){const v=window.visualViewport;return {left:v?v.offsetLeft:0,top:v?v.offsetTop:0,width:v?v.width:innerWidth,height:v?v.height:innerHeight}}
function highlightNode(node){if(!node)return null;const headingId=node.getAttribute("aria-labelledby");return headingId?document.getElementById(headingId)||node:node}
function setRect(node,left,top,width,height){node.style.left=`${left}px`;node.style.top=`${top}px`;node.style.width=`${Math.max(0,width)}px`;node.style.height=`${Math.max(0,height)}px`}
function positionDimmers(v,hole){dimmers.forEach(node=>node.classList.remove("hidden"));if(!hole){setRect(dimmers[0],v.left,v.top,v.width,v.height);for(let i=1;i<dimmers.length;i++)setRect(dimmers[i],v.left,v.top,0,0);return}const right=v.left+v.width,bottom=v.top+v.height;setRect(dimmers[0],v.left,v.top,v.width,hole.top-v.top);setRect(dimmers[1],hole.right,hole.top,right-hole.right,hole.height);setRect(dimmers[2],v.left,hole.bottom,v.width,bottom-hole.bottom);setRect(dimmers[3],v.left,hole.top,hole.left-v.left,hole.height)}
function positionTour(){if(!active)return;const v=viewport(),gap=16,pad=16,panelRect=panel.getBoundingClientRect();let left=v.left+(v.width-panelRect.width)/2,top=v.top+(v.height-panelRect.height)/2;if(target){const r=target.getBoundingClientRect(),spotPad=7,hole={left:Math.max(v.left+4,r.left-spotPad),top:Math.max(v.top+4,r.top-spotPad),right:Math.min(v.left+v.width-4,r.right+spotPad),bottom:Math.min(v.top+v.height-4,r.bottom+spotPad)};hole.width=Math.max(0,hole.right-hole.left);hole.height=Math.max(0,hole.bottom-hole.top);positionDimmers(v,hole);spotlight.classList.remove("hidden");setRect(spotlight,hole.left,hole.top,hole.width,hole.height);const rightSpace=v.left+v.width-r.right,leftSpace=r.left-v.left,belowSpace=v.top+v.height-r.bottom,aboveSpace=r.top-v.top;if(rightSpace>=panelRect.width+gap)left=r.right+gap;else if(leftSpace>=panelRect.width+gap)left=r.left-panelRect.width-gap;else if(belowSpace>=panelRect.height+gap)top=r.bottom+gap;else if(aboveSpace>=panelRect.height+gap)top=r.top-panelRect.height-gap;else top=belowSpace>=aboveSpace?v.top+v.height-panelRect.height-pad:v.top+pad}else{spotlight.classList.add("hidden");positionDimmers(v,null)}panel.style.left=`${Math.max(v.left+pad,Math.min(left,v.left+v.width-panelRect.width-pad))}px`;panel.style.top=`${Math.max(v.top+pad,Math.min(top,v.top+v.height-panelRect.height-pad))}px`}
function schedulePosition(){cancelAnimationFrame(positionFrame);positionFrame=requestAnimationFrame(positionTour)}
function clearTarget(){target=null;spotlight.classList.add("hidden");dimmers.forEach(node=>node.classList.add("hidden"))}
function render(){clearTarget();const step=steps[current],candidate=step.target?[...document.querySelectorAll(`[data-onboarding-target="${step.target}"]`)].find(node=>node.offsetParent!==null):null;target=highlightNode(candidate);title.textContent=step.title;text.textContent=step.text+(step.target&&!candidate?" This item is not available in the current layout; you can continue safely.":"");progress.textContent=`Step ${current+1} of ${steps.length}`;back.disabled=current===0;next.textContent=current===steps.length-1?"Finish":"Next";if(target)target.scrollIntoView({behavior:matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth",block:"center",inline:"nearest"});schedulePosition();setTimeout(schedulePosition,220);panel.focus()}
async function end(action){await api("/"+action);active=false;clearTarget();modal.classList.add("hidden");document.body.classList.remove("onboarding-active");if(previousFocus)previousFocus.focus();window.dispatchEvent(new CustomEvent("onboarding:ready",{detail:{active:false}}))}
async function start(state){active=true;window.dispatchEvent(new CustomEvent("onboarding:ready",{detail:{active:true}}));current=Math.min(steps.length-1,state.current_step||0);previousFocus=document.activeElement;modal.classList.remove("hidden");document.body.classList.add("onboarding-active");if(state.status==="not_started")await api("/start");render()}
back.addEventListener("click",async()=>{if(current){current--;await api("/step","PATCH",{current_step:current});render()}});next.addEventListener("click",async()=>{if(current===steps.length-1){await end("complete");return}current++;await api("/step","PATCH",{current_step:current});render()});skip.addEventListener("click",()=>end("skip"));
modal.addEventListener("keydown",e=>{if(e.key==="Escape"){e.preventDefault();skip.focus();return}if(e.key!=="Tab")return;const n=focusables();if(!n.length)return;const f=n[0],l=n[n.length-1];if(e.shiftKey&&document.activeElement===f){e.preventDefault();l.focus()}else if(!e.shiftKey&&document.activeElement===l){e.preventDefault();f.focus()}});
window.MyBeaconOnboarding={replay:async()=>start(await api("/replay"))};
addEventListener("resize",schedulePosition);addEventListener("orientationchange",schedulePosition);if(window.visualViewport){visualViewport.addEventListener("resize",schedulePosition);visualViewport.addEventListener("scroll",schedulePosition)}
window.addEventListener("load",async()=>{try{const r=await fetch("/api/portal/onboarding",{credentials:"include"});if(!r.ok)throw 0;const state=await r.json();if(state.should_start)await start(state);else window.dispatchEvent(new CustomEvent("onboarding:ready",{detail:{active:false}}))}catch(_){window.dispatchEvent(new CustomEvent("onboarding:ready",{detail:{active:true}}))}},{once:true});
})();
